# 1 = every api.ts call is served from frontend/lib/mocks instead of the backend.
NEXT_PUBLIC_USE_MOCKS=1
# Backend base URL, used when NEXT_PUBLIC_USE_MOCKS is not 1.
NEXT_PUBLIC_API_BASE=http://localhost:8000
