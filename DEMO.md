# Demo

Stub. **W12 writes this.** The shot list, timings and running order are
`PLAN.md` §0.6 — follow it there, not from memory:

1. (20s) The agent, its GitHub tools, its empty memory. v0 on one issue.
2. (30s) Reflection writes rules + a tool note from tool data, with evidence.
3. (25s) Gate accepts; the band moves up; the fix card shows the entries added.
4. (25s) Compare page: the same issue at v0 vs vN.
5. (25s) Insights: pass-rate band, memory growth, tool efficiency.
6. (20s) A rejected fix and a demoted rule.
7. (20s) Holdout — the newest issues — then Domain B and the playbook ablation.
8. (10s) AO board + `BUILD_LOG.md` + repo link.

Target run time ~3:05; trim shot 5 if needed.

Setup commands and env vars: `README.md`.
