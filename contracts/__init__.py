"""Frozen contracts shared by every workstream.

`contracts/` is frozen after Phase 0 merges. A change requires a GitHub issue
labelled `contract-change` and an orchestrator decision (PLAN.md §2.3).
"""
